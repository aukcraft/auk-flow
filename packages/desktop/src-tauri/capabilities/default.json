{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "AukFlow 主窗口默认能力：仅允许核心事件，无 fs/shell/network 直通",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
